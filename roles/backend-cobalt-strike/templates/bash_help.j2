GREEN='\033[0;32m'
NC='\033[0m' # No Color
echo -e "${GREEN}- Cobalt Strike Docker for {{ identification }}${NC}"
echo '  - Shells are received via {{ relay_host }} on {{ relay_host_ip }}'
echo '  - Use "ssh -L 50050:localhost:50050 {{ansible_host}}" to set up a tunnel'
echo '    Then connect your local cobaltstrike to localhost port 50050'
